#include <stdio.h>

/**
 * main - function of the task
 * Return: always zero
 */

int main(void)
{ char i;

	for (i = 'a'; i <= 'z'; i++)
	putchar(i);

	for (m = 'A'; m <= 'Z'; m++)
	putchar (m);

	putchar ('\n');

	return (0);

}
